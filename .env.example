# Optional: if set, this Client ID pre-fills the "Google OAuth Client ID" field
# for every visitor, so they only have to click "Đăng nhập & thêm vào Google Calendar".
# Users can still override it manually in the UI.
# See README.md "Thiết lập Google OAuth Client ID" for how to create one.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
